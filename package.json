{
  "name": "linuxverse",
  "version": "1.0.0",
  "description": "Linux distribution discovery website — static site generator",
  "scripts": {
    "build": "node build.js",
    "update": "node scripts/update-versions.js",
    "dev": "node build.js && npx serve docs"
  },
  "engines": { "node": ">=18" },
  "license": "MIT"
}
